services:
  household-panel:
    build:
      context: .
      dockerfile: Dockerfile
    image: household-panel
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
