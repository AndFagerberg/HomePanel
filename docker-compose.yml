services:
  household-panel:
    build:
      context: .
      dockerfile: Dockerfile
    image: household-panel
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - homepanel-data:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production

volumes:
  homepanel-data:
