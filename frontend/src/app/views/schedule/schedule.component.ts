import { Component } from '@angular/core';

// Configurable household schedule view (see PROJECT.md section 12).
@Component({
  selector: 'app-schedule-view',
  standalone: true,
  template: `<div class="placeholder-view">Schedule view</div>`,
})
export class ScheduleComponent {}
